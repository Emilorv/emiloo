version: '3'
services:
  vue-app:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    networks:
      - nginx-proxy-network

networks:
  nginx-proxy-network:
    external: true
